#here we will perform some basic mathematical operations on numpy arrays
import numpy as np
arr=np.array([1,2,3,4,5])
# print(arr+5)#adds 5 to each element of the array
# print(arr-2)#subtracts 2 from each element of the array
# print(arr*2)#multiplies each element of the array by 2
# print(arr/2)#divides each element of the array by 2
# print(arr%2)#gives the remainder when each element of the array is divided by 2
# print(arr**2)#raises each element of the array to the power of 2
# print(arr//2)#gives the quotient when each element of the array is divided by 2

#now we perfom aggregate functions on numpy arrays
# print(np.sum(arr))#gives the sum of all the elements in the array
# print(np.mean(arr))#gives the mean of all the elements in the array
# print(np.median(arr))#gives the median of all the elements in the array
# print(np.std(arr))#gives the standard deviation of all the elements in the array
# print(np.var(arr))#gives the variance of all the elements in the array
# print(np.min(arr))#gives the minimum element in the array
# print(np.max(arr))#gives the maximum element in the array
# print(np.argmin(arr))#gives the index of the minimum element in the array
# print(np.argmax(arr))#gives the index of the maximum element in the array
# print(np.sqrt(arr))#gives the square root of each element in the array
# print(np.sin(arr))#gives the sine of each element in the array
# print(np.cos(arr))#gives the cosine of each element in the array
# print(np.tan(arr))#gives the tangent of each element in the array
# print(np.log(arr))#gives the natural logarithm of each element in the array
# print(np.exp(arr))#gives the exponential of each element in the array
# print(np.cumsum(arr))#gives the cumulative sum of all the elements in the
# print(np.cumprod(arr))#gives the cumulative product of all the elements in the array
# print(np.cummax(arr))#gives the cumulative maximum of all the elements in
# print(np.cummin(arr))#gives the cumulative minimum of all the elements in the array
# print(np.sort(arr))#sorts the array in ascending order
# print(np.unique(arr))#gives the unique elements in the array
# print(np.diff(arr))#gives the difference between each element in the array
# print(np.round(arr))#rounds each element in the array to the nearest integer
# print(np.floor(arr))#gives the largest integer less than or equal to each element
# print(np.ceil(arr))#gives the smallest integer greater than or equal to each element
# print(np.clip(arr,2,4))#clips the values in the array to be
print(np.sum(arr))#gives the sum of all the elements in the array
print(np.mean(arr))#gives the mean of all the elements in the array
print(np.median(arr))#gives the median of all the elements in the array
print(np.std(arr))#gives the standard deviation of all the elements in the array
print(np.var(arr))#gives the variance of all the elements in the array
print(np.min(arr))#gives the minimum element in the array
print(np.max(arr))#gives the maximum element in the array
print(np.argmin(arr))#gives the index of the minimum element in the array
print(np.argmax(arr))#gives the index of the maximum element in the array
print(np.sqrt(arr))#gives the square root of each element in the array
print(np.sin(arr))#gives the sine of each element in the array
print(np.cos(arr))#gives the cosine of each element in the array
print(np.tan(arr))#gives the tangent of each element in the array
print(np.log(arr))#gives the natural logarithm of each element in the array
print(np.exp(arr))#gives the exponential of each element in the array
print(np.cumsum(arr))#gives the cumulative sum of all the elements in the
print(np.cumprod(arr))#gives the cumulative product of all the elements in the
#print(np.cummax(arr))#gives the cumulative maximum of all the elements in--->not supported in this version of numpy
#print(np.cummin(arr))#gives the cumulative minimum of all the elements in the--->not supported in this version of numpy
print(np.sort(arr))#sorts the array in ascending order
print(np.unique(arr))#gives the unique elements in the array
print(np.diff(arr))#gives the difference between each element in the array
print(np.round(arr))#rounds each element in the array to the nearest integer
print(np.floor(arr))#gives the largest integer less than or equal to each element
print(np.ceil(arr))#gives the smallest integer greater than or equal to each element
print(np.clip(arr,2,4))#clips the values in the array to be between
